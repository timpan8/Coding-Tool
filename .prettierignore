# The terse style in src is deliberate; Prettier must never rewrite it.
*.ts
*.tsx
*.mjs
dist/
pnpm-lock.yaml
# CodeVault is a separate app with its own toolchain (npm, ESLint, Vitest); see codevault/README.md.
codevault/
